# Arbres-ID — instructions pour Claude

## Git

**Ne jamais committer.** L'utilisateur gère tous les commits lui-même.
Faire les modifications de fichiers, puis s'arrêter — ne pas lancer `git add` ni `git commit`.
